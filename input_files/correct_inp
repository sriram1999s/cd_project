#include <stdio.h>
#include "temp.h"
void foo();
int main()
{
  int a;
  int b  = 5;
  
  if(a<5)
  {
    if(j>2)
    while(k<10)
    --k;
    if(h==3)
    ++h;
    else
    {
      i+=7;
    }
    p++;
  }
  else
  d*=7;
}
