#include <stdio.h>
#include "temp.h"
void foo();
int main()
{
  int a;
  int b  = 5;
  
  if(a<5)
  {
    if(j>2)
    while(k<10)
    --k;
    if(h==3)
    ++h;
    else
    {
      i+=7;
    }
    p++;
  }
  else
  d*=7;
  x=y*z;
  p=p+1;
  q=p-4;
  int r=-7;
  int t=s*r/q+w-u;
}
